# ${{ values.name }} - ERPNext GitOps repo

Generated by the Backstage `erpnext-deployment` template.

- **Site:** https://${{ values.siteName }}
- **Environment:** ${{ values.environment }}
- **Owner:** ${{ values.owner }}
- **Vault path:** `${{ values.vaultPath }}`

## Layout
```
helm/values.yaml          # frappe/erpnext Helm values
manifests/                # namespace, ExternalSecret, network policies
argocd/application.yaml   # ArgoCD Application (GitOps)
.github/workflows/        # deploy + compliance drift
docs/                     # TechDocs (runbook, IR, compliance, access)
catalog-info.yaml         # Backstage catalog (System + Component + Resources)
```

## Pre-deploy checklist
- [ ] Vault path `${{ values.vaultPath }}` populated (`admin_password`, `db_root_password`, `encryption_key`)
- [ ] DNS `${{ values.siteName }}` points at the ingress controller
- [ ] cert-manager ClusterIssuer available for TLS
- [ ] ExternalSecrets operator running, `ClusterSecretStore/vault-backend` exists
- [ ] Promtail / Alloy scraping namespace `${{ values.name }}`
- [ ] Grafana dashboard `erpnext` imported (UID `erpnext`)
- [ ] PagerDuty service exists for owner team
- [ ] `compliance.iso27001/last-reviewed` updated to today

## First deploy
Run the **Deploy ERPNext (Helm)** workflow with `action=install`.
Subsequent changes go through PR → ArgoCD reconciles automatically.
